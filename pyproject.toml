[project]
name = "readme-to-pdf"
version = "0.1.0"
description = "Generate a PDF from README markdown"
requires-python = ">=3.14"
dependencies = [
    "markdown",
    "pdfkit",
]

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
